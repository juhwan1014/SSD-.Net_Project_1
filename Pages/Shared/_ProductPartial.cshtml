@model SSDeShopOnWeb.Models.Product
<div class="col-md-4">
    <img src="@Model.PictureUri" />
    <input type="submit" value="Add to Cart" />
    <span>@Model.Name</span>
    <span>@Model.Price.ToString("C")</span>
</div>